The visio module is where all the code lives for implementing the rules engine. 

run_tests.py can be executed to pull in the unit and systems tests and print the results.

Example usage of the module can be found in the design doc.